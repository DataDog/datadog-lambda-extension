image: registry.ddbuild.io/images/mirror/python:3.9

stages:
  - first
  - second

first-job:
  stage: first
  tags: ["runner:main"]
  variables:
    PIPELINE_EXECUTION_ROLE: {{pipeline_execution_role}}
  script:
    - pip3 install awscli==1.27.85
    - aws lambda list-layer-versions --layer-name Datadog-Extension

second-job:
  stage: second
  tags: ["runner:main"]
  script:
    - echo "Test second step"